#include <stdio.h>

// Incomplete
int main(){

	int numbers[50], limit;

	printf("Enter the Limit of number: ");
	scanf("%d", &limit);

	printf("\n");
	for (i=0; i<lmit; i++){
		printf("Enter the number: ");
		scanf("%d", &number[i]);

		if(number[i] == 
	}

	return 0;
}
